import { Meta, StoryObj } from '@storybook/react';

import { CTAWell } from './cta-well';

const meta: Meta<typeof CTAWell> = {
  component: CTAWell,
};

export default meta;

type Story = StoryObj<typeof CTAWell>;

export const Default: Story = {};
